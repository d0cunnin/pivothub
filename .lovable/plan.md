## Plan: Redeploy all edge functions

List every function directory under `supabase/functions/` (excluding hidden `_shared` etc.) and redeploy them in one batch via the deploy tool. Report success/failures.

No code changes.